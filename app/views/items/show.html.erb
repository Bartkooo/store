<%= render @item %>

<div>
  <%= link_to "Edit this item", edit_item_path(@item) %> |
  <%= link_to "Back to items", items_path %> |
  <%= link_to "Destroy this item", @item, method: :delete, data: { confirm: 'Are you sure?', turbo_method: :delete } %>
</div>
