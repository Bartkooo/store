<h1>Items</h1>

<div id="items">
  <% @items.each do |item| %>
    <%= render item %>
  <% end %>
</div>

<% if user_signed_in? %>
  <%= link_to "New item", new_item_path %>
<% else %>
  <%= link_to "New item", new_user_session_path %>
<% end %>